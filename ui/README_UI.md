# Руководство по интерфейсу и тестированию

## 🎨 Система тем

Проект поддерживает светлую и темную темы оформления. Все стили находятся в директории `ui/themes/stylesheets/`.

### Доступные темы:
- **light** - светлая тема
- **dark** - темная тема

## 🧪 Запуск тестов

### 1. Тестирование системы тем
```bash
python test_theme.py
```
Этот тест демонстрирует:
- Окно для смены и применения тем
- Автоматическое определение доступных тем
- Переключение между светлой и темной темами

**Скриншоты:**
1. Окно по умолчанию
2. Светлая тема
3. Темная тема

### 2. Интеграционный тест криптосистемы и UI
```bash
python test_crypto_integration.py
```
Этот тест проверяет:
- Импорт и работу модуля `medical_crypto.py`
- Совместную работу криптосистемы и системы тем
- Доступность всех API-методов для UI
- Структуры данных (DoctorInfo, PatientInfo, SessionInfo)

## 🔧 Структура интеграции

### Основные компоненты:

1. **MedicalCryptoFacade** - фасад криптосистемы
   - Регистрация врачей
   - Аутентификация
   - Управление пациентами
   - Шифрование/дешифрование медицинских записей

2. **ThemeManager** - менеджер тем
   - Загрузка QSS-стилей
   - Переключение между темами
   - Применение стилей к приложению

3. **QSSThemeProvider** - провайдер тем
   - Сканирование директории с темами
   - Регистрация доступных тем
   - Управление QSS-файлами

### API для UI-виджетов:

- **LoginWidget** → `medical_crypto.login_doctor()`
- **RegistrationWidget** → `medical_crypto.register_doctor()`
- **PatientForm** → `medical_crypto.add_patient()`

## 📊 Статус системы

На момент тестирования:
- ✅ Криптосистема готова к интеграции
- ✅ Система тем функционирует
- ✅ Все необходимые API-методы доступны
- ✅ Структуры данных подготовлены для UI

## 🚀 Следующие шаги

1. Создание `LoginWidget` с использованием `medical_crypto.login_doctor()`
2. Реализация `RegistrationWidget` с `medical_crypto.register_doctor()`
3. Разработка формы пациентов с `medical_crypto.add_patient()`
4. Интеграция ThemeManager в главное окно приложения

## ⚠️ Известные проблемы

- При применении темы возникает ошибка: `'NoneType' object has no attribute 'setStyleSheet'`
  - **Решение**: Убедитесь, что объект приложения инициализирован перед применением темы

Система готова к разработке пользовательского интерфейса! Все основные компоненты протестированы и работают корректно.

<div align="center">
Позже будет загружен код. Сейчас можно будет ознакомится с `'README_UI.md'`
</div>